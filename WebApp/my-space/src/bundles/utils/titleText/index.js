import React from "react";
import Typography from "@material-ui/core/Typography";

const TitleText = ({ text }) => (
  <Typography style={{ fontWeight: 600, fontSize: 26 }}>{text}</Typography>
);

export default TitleText;
